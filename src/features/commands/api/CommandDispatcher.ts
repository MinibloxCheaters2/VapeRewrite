import { CommandDispatcher } from "@wq2/brigadier-ts";

// export class CommandSource {
// 	// idk if we need any things here, but probably not.
// }

export default new CommandDispatcher<unknown>();
