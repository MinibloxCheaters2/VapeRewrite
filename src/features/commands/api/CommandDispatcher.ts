import { CommandDispatcher } from "@wq2/brigadier-ts";


export class CommandSource {
	// TODO: idk if we need any things here, but probably not.
}

export default new CommandDispatcher<CommandSource>();
